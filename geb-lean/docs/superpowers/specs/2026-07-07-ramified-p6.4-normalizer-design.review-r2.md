# Adversarial review: sub-phase 6.4 normalizer design (round 2)

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
**Table of Contents**  *generated with [DocToc](https://github.com/thlorenz/doctoc)*

- [Summary](#summary)
- [Round-1 resolution verification](#round-1-resolution-verification)
  - [M1. `collapseER` atomic-builder / 6.5b tupling record](#m1-collapseer-atomic-builder--65b-tupling-record)
  - [N1. `RType` arrow shorthand](#n1-rtype-arrow-shorthand)
  - [N2. "bulkiest"](#n2-bulkiest)
  - [N3. "hard spots"](#n3-hard-spots)
- [Fresh-sweep findings](#fresh-sweep-findings)
- [Tooling](#tooling)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

Verdict: CONVERGED (0 MAJOR, 0 MINOR, 0 NIT).

Target:
`docs/superpowers/specs/2026-07-07-ramified-p6.4-normalizer-design.md`.
Round-1 report:
`docs/superpowers/specs/2026-07-07-ramified-p6.4-normalizer-design.review-r1.md`.
Scope this round: verify the four round-1 resolutions, one fresh
convergence sweep (not re-verifying round-1's audited items), and the
markdownlint/doctoc gates.

## Summary

All four round-1 resolutions (M1 MINOR, N1/N2/N3 NIT) are present in the
current spec text and are accurate against the parent sub-plan. The
fresh sweep surfaced no internal contradictions introduced by the edits,
no remaining plan-blocking ambiguity, no style-rule violations, and no
factually doubtful claim. markdownlint reports 0 errors and the doctoc
TOC is current. The spec has converged.

## Round-1 resolution verification

### M1. `collapseER` atomic-builder / 6.5b tupling record

Resolved. Section 6.4 now records, verbatim, all three required
elements: `collapseER` is "the atomic builder for a single codomain-`o`
source morphism (output arity `1`)"; the 6.5b `collapseFunctor` "obtains
its general morphism map by applying `collapseER` to each of the
`objLen Δ` codomain components and tupling into
`ERMorN (objLen Γ) (objLen Δ)` (tupling is definitional for `ERMorN`)";
and "a tupling lemma lifting the per-component `collapseER_interp` to the
parent's whole-morphism `collapseDenotation g`; that tupling layer is
6.5 scope."

Section 6.3 no longer claims the "`ERMorN a 1` shape 6.5 consumes"
phrasing (grep for `shape 6.5 consumes` returns nothing); its closing
paragraph now speaks only of the unary case and the `a`-ary input form.

Accuracy against the parent plan: parent §6.5a pins
`collapseDenotation : (Fin (objLen Γ) → ℕ) → (Fin (objLen Δ) → ℕ)`
(`objLen Δ` outputs), and the consumed-interface block pins
`def ERMorN (n m : ℕ) : Type := Fin m → ERMor1 n`, so a tuple of
`objLen Δ` per-component morphisms is exactly an
`ERMorN (objLen Γ) (objLen Δ)` and the tupling is definitional. Parent
§6.5b's morphism map ("the 6.4 `collapseER` of the morphism's
applicative translation") and adequacy bridge (the landed `ERMorN`
interp equals `collapseDenotation g`) match the spec sentence. The
input arity `a` in the §6.4 signature `ERMorN a 1` equals `objLen Γ`,
which the M1 sentence makes explicit by naming `ERMorN (objLen Γ) …`.
The record is accurate and closes the consumer-fit gap.

### N1. `RType` arrow shorthand

Resolved. The `⟶` shorthand is absent from the spec (grep for `⟶`
returns nothing). Section 6.3's fixed-source signature now reads
`RType.arrow (RType.omega τ) RType.o`, matching `prop13_elementary`'s
repository spelling; the §6.4 blocks that inherited it carry no arrow
notation.

### N2. "bulkiest"

Resolved. "bulkiest" is absent (grep returns nothing). Section 6.1 now
reads "the largest proof obligation of the sub-phase" and §8.2 "the
largest arm-by-arm mirror".

### N3. "hard spots"

Resolved. "hard spots" is absent (grep returns nothing). Section 8.2
opens "Two flagged difficulty concentrations", matching its own heading.

## Fresh-sweep findings

None. A full re-read against the round-1 audit boundary produced no new
findings at any severity.

- Internal consistency of the M1 edit: the new §6.4 sentence uses
  `objLen Γ`/`objLen Δ` where the surrounding signatures use `a`; the
  two are reconciled in the same sentence (`ERMorN (objLen Γ) …`), so no
  contradiction is introduced. `collapseER : ERMorN a 1` (output arity
  `1`), `collapseER_interp` over `v : Fin a → ℕ`, and the §6.3
  `buildCode`/`clockER : ERMor1 1` unary-first framing remain mutually
  consistent.
- Style: the value-laden-adjective / colloquialism / hedge scans
  (`key`, `crucial`, `elegant`, `powerful`, `leverage`, `tricky`,
  `straightforward`, `blocked`, `issue`, `challenge`, and the like) all
  return nothing. The edits introduced no new prose defects.
- No new factual claim was introduced by the resolutions beyond the
  `ERMorN`/`collapseDenotation` typing already verified under M1;
  round-1's "Verified without defect" repo-factual audit is not redone
  here per instruction.

## Tooling

- `markdownlint-cli2` on the target: `Summary: 0 error(s)`.
- `doctoc --dryrun --update-only` on the target: "Everything is OK"
  (TOC current).
- This report: markdownlint-clean, doctoc TOC present and current.
