# Adversarial review: sub-phase 6.4 normalizer mini-sub-plan (round 3)

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
**Table of Contents**  *generated with [DocToc](https://github.com/thlorenz/doctoc)*

- [Resolution verification](#resolution-verification)
- [Regression sweep](#regression-sweep)
- [Style and TOC checks](#style-and-toc-checks)
- [Summary](#summary)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

Verdict: CONVERGED (0 MAJOR, 0 MINOR, 0 NIT).

Target:
`docs/superpowers/plans/2026-07-07-ramified-p6.4-normalizer-subplan.md`.
Round-2 report:
`docs/superpowers/plans/2026-07-07-ramified-p6.4-normalizer-subplan.review-r2.md`
(0 MAJOR, 1 MINOR, 0 NIT). Scope this round: verify the single
round-2 MINOR resolution (File-structure import list), confirm no
regression elsewhere, and re-run the style/TOC checks.

## Resolution verification

The File structure section's `CodeNormalizer.lean` import list
(lines 255-262) now reads:

> Imports `CodeTm.lean`, `OneLambdaEval.lean`,
> `GebLean/Utilities/ERArith.lean`, `ERTupling.lean`,
> `GebLean/Utilities/ERTreeArith.lean` (the `ERMor1.foldBTLOnCode`
> combinator, decision P3), `LawvereERKSim/ErToKFunctor.lean`.

`LawvereTreeERArith` no longer appears in the import list, and
`GebLean/Utilities/ERTreeArith.lean` is now named with an explicit
pointer to `ERMor1.foldBTLOnCode` and a cross-reference to decision
P3. This matches the round-2 fix recommendation exactly.

Source check: `GebLean/Utilities/ERTreeArith.lean` exists and
`def ERMor1.foldBTLOnCode` is declared at line 615, consistent with
the anchor pinned throughout the rest of the plan (Decision P3
lines 137-139, Consumed interfaces line 239, Note N4 line 351, Task
6.4.12 line 929).

## Regression sweep

A full-file grep for `LawvereTreeERArith`/`ERTreeArith` finds seven
occurrences:

- Line 138, 239, 260, 351, 929: `GebLean/Utilities/ERTreeArith.lean`
  (the corrected anchor), consistent with the round-2 verified
  locations plus the now-fixed import list.
- Line 142 (Decision P3) and line 360 (Note N4): the two permitted
  demotion parentheticals — `treeFoldOnCode`/`mutuFoldOnCode` in
  `LawvereTreeERArith.lean`, explicitly labelled "background
  precedent only; they do not lower to `ERMor1`" (P3) and
  "background only" (N4). No other occurrence of
  `LawvereTreeERArith` exists in the plan.

No other content in the File structure section changed; the
remaining bullets (`DetStep.lean`, `CodeTm.lean` creation, test
wiring) are unaffected and untouched.

## Style and TOC checks

- `markdownlint-cli2` on the plan: `Summary: 0 error(s)`.
- `doctoc --dryrun --update-only` on the plan: "Everything is OK"
  (TOC current — the file has zero `##` headings, so no TOC block
  is expected or present).

## Summary

The sole round-2 MINOR finding — the File-structure import list
naming the demoted `LawvereTreeERArith` module instead of the
`ERTreeArith.lean` anchor — is resolved exactly as recommended, and
verified against source (`ERMor1.foldBTLOnCode` at
`ERTreeArith.lean:615`). The regression sweep finds no other
`LawvereTreeERArith` occurrence beyond the two permitted background
demotions in Decision P3 and Note N4. Style and TOC checks pass
clean. Verdict: CONVERGED.
