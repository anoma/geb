# Adversarial review: sub-phase 6.4 normalizer mini-sub-plan (round 2)

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
**Table of Contents**  *generated with [DocToc](https://github.com/thlorenz/doctoc)*

- [Resolution verification](#resolution-verification)
  - [M1 (course-of-values precedent) — resolved, one leftover](#m1-course-of-values-precedent--resolved-one-leftover)
  - [M2 (N2 payload-positivity / `pair_one_lt`) — resolved](#m2-n2-payload-positivity--pair_one_lt--resolved)
  - [N1 (Decision P4 phrasing) — resolved](#n1-decision-p4-phrasing--resolved)
- [Fresh-sweep findings](#fresh-sweep-findings)
  - [MINOR — File-structure import list still names the demoted module](#minor--file-structure-import-list-still-names-the-demoted-module)
  - [Other sweep results — clean](#other-sweep-results--clean)
- [Style and TOC checks](#style-and-toc-checks)
- [Summary](#summary)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

Verdict: ITERATE (0 MAJOR, 1 MINOR, 0 NIT).

Target:
`docs/superpowers/plans/2026-07-07-ramified-p6.4-normalizer-subplan.md`.
Round-1 report:
`docs/superpowers/plans/2026-07-07-ramified-p6.4-normalizer-subplan.review-r1.md`
(0 MAJOR, 2 MINOR, 1 NIT). Scope this round: verify the three
round-1 resolutions (M1, M2, N1), one fresh convergence sweep,
and the style/TOC checks. Repository read at branch tip.

## Resolution verification

### M1 (course-of-values precedent) — resolved, one leftover

The anchor swap to `ERMor1.foldBTLOnCode` landed in five of the
six mandated locations, each verified against source:

- Decision P3 (lines 137-142): names `ERMor1.foldBTLOnCode`
  (`GebLean/Utilities/ERTreeArith.lean:615`) with ℕ-reference
  `Nat.foldBTLOnCode` (`GebLean/Utilities/SzudzikSeq.lean:149`),
  describes the table as Gödel β-encoded, and demotes
  `treeFoldOnCode`/`mutuFoldOnCode` to "background precedent
  only; they do not lower to `ERMor1`". Correct.
- Note N4 (lines 350-362): same anchor and line pins; describes
  the combinator as shipping an unconditional upper bound and a
  reference-correctness lemma conditioned on `bound` domination;
  demotes the tree-category folds. Correct.
- Tech Stack (line 72): `ERMor1.foldBTLOnCode`. Correct.
- Consumed interfaces (lines 238-239):
  `ERMor1.foldBTLOnCode` (`Utilities/ERTreeArith.lean:615`) with
  reference `Nat.foldBTLOnCode` (`Utilities/SzudzikSeq.lean:149`).
  Correct.
- Task 6.4.12 Step 1 (lines 927-928): re-checkpoint reads
  `ERMor1.foldBTLOnCode` and `Nat.foldBTLOnCode` at the two
  pinned lines "their bound and correctness lemmas". Correct.

Source cross-check: `def ERMor1.foldBTLOnCode` is at
`ERTreeArith.lean:615` (docstring opens at 607); its bound lemma
`ERMor1.interp_foldBTLOnCode_le_bound` (:638) is unconditional
(`.interp ctx ≤ bound.interp ctx`) and its correctness helper
`ERMor1.foldBTLPred_trace_match` (:653) is conditioned on a trace
domination hypothesis — the plan's "unconditional bound plus
conditional correctness" description is accurate. The ER body
composes `ERMor1.beta` (:623), matching the "Gödel β-encoding"
table claim. `def foldBTLOnCode` sits inside `namespace Nat`
(lines 24-231) at `SzudzikSeq.lean:149`, so `Nat.foldBTLOnCode`
resolves. `treeFoldOnCode`/`mutuFoldOnCode` remain in the
distinct `GebLean/LawvereTreeERArith.lean` module, which imports
`LawvereTreeERInterp`/`SzudzikSeq`/`RegisterMachine` and never
reaches `ERTreeArith`, confirming the "do not lower to `ERMor1`"
demotion.

The sixth location — the File-structure import list — was not
swept; see the MINOR finding below.

### M2 (N2 payload-positivity / `pair_one_lt`) — resolved

- N2 (lines 324-333) no longer claims `payload ≥ 1`. It states
  `p < Nat.pair 1 p` holds unconditionally, with the correct
  two-branch arithmetic: `p ≤ 1` gives value `2 + p` (mathlib
  `Nat.pair 1 p = 1·1 + 1 + p = p + 2` when `¬ 1 < p`), and
  `1 < p` gives value `p·p + 1`. Both exceed `p`; the branches
  partition ℕ on the `1 < p` test that defines `Nat.pair`.
  Correct against `Nat.pair a b = if a < b then b*b+a else
  a*a+a+b`.
- Task 6.4.6 (line 653): `pair_one_lt (p : ℕ) : p < Nat.pair 1 p`
  carries no `1 ≤ p` hypothesis. Unconditional as required. The
  surrounding strictness-cluster prose (lines 660-667) makes no
  positivity claim.

### N1 (Decision P4 phrasing) — resolved

P4 (lines 148-160) no longer says the right-hand side is
"exactly the two sides" of `prop13_elementary`. It now reads "the
applied term of `prop13_elementary`
(`Normalization.lean:3103-3110`) and the numeral read-off
(through `decodeWord`) of the word inside its `conc` normal
form". Verified against source: `prop13_elementary` is declared
at `Normalization.lean:3103`, its reduction target is
`conc (appEval (sourceApp F (sourceWord a τ)) finZeroElim)`, and
the adequacy RHS reads that word off via `decodeWord`/
`freeAlgToNat`. The description is accurate.

## Fresh-sweep findings

### MINOR — File-structure import list still names the demoted module

Location: File structure, line 260.

The M1 fix relocated the course-of-values anchor to
`ERMor1.foldBTLOnCode` in `GebLean/Utilities/ERTreeArith.lean`
and demoted `LawvereTreeERArith` (which houses
`treeFoldOnCode`/`mutuFoldOnCode`) to background-only, stating it
"do[es] not lower to `ERMor1`". The File-structure import list
for `CodeNormalizer.lean` was not updated to match: it still
lists

> Imports `CodeTm.lean`, `OneLambdaEval.lean`,
> `GebLean/Utilities/ERArith.lean`, `ERTupling.lean`,
> `LawvereTreeERArith` precedent module,
> `LawvereERKSim/ErToKFunctor.lean`.

This is internally inconsistent with the M1-corrected plan:

1. It names `LawvereTreeERArith` — the module the same plan now
   labels background-only and non-lowering — as a required
   import, while omitting `GebLean/Utilities/ERTreeArith.lean`,
   the module the plan now anchors its hardest task on.
2. `ERTreeArith` is not transitively reachable from any listed
   import: `ERArith`/`ERTupling` do not import it, and
   `LawvereTreeERArith`/`ErToKFunctor` do not either (verified by
   grep of their import headers). So an implementer following the
   File structure would set up imports that do not provide
   `ERMor1.foldBTLOnCode`.

Evidence: plan line 260 (import list) versus lines 137-139,
238-239, 350-351, 927-928 (anchor at `ERTreeArith.lean:615`);
`grep '^import' GebLean/LawvereTreeERArith.lean` yields
`LawvereTreeERInterp`, `SzudzikSeq`, `RegisterMachine` (no
`ERTreeArith`); `GebLean/Utilities/{ERArith,ERTupling}.lean` do
not import `ERTreeArith`.

Impact: bounded but real. Task 6.4.12 Step 1's re-checkpoint
sends the implementer to `ERTreeArith.lean:615`, so the correct
module is discoverable and import lists are advisory rather than
binding — hence MINOR, not MAJOR. The fix is to replace
"`LawvereTreeERArith` precedent module" with
"`GebLean/Utilities/ERTreeArith.lean` (`ERMor1.foldBTLOnCode`)"
in the import list (dropping or retaining `LawvereTreeERArith`
only if a background read is intended, but it need not be
imported for that).

### Other sweep results — clean

- No other occurrence of the stale anchor: a full grep of the
  plan for `LawvereTreeERArith`/`treeFoldOnCode`/`mutuFoldOnCode`/
  `seqPack`/`seqAt` returns only the two demotion parentheticals
  (P3 line 142, N4 line 359) and the line-260 import; the
  round-1 "Szudzik seqPack/seqAt" packing language is gone.
- The five downstream-consumed course-of-values statements
  (`normStep`, `normStep_interp`, `normRun_*`) are unchanged by
  the edits and remain consistent with the corrected N4/P3.
- No new contradiction between the M2 edit and Task 6.4.5's
  `codeRType RType.o = Nat.pair 0 0 = 0`: the unconditional
  `pair_one_lt` covers the payload-`0` node the round-1 report
  flagged.
- Decisions P1-P6, the Execution-notes latitude list, and the
  self-review checklist are internally consistent with the
  edited notes; no name drift introduced.

## Style and TOC checks

- `markdownlint-cli2` on the plan: `Summary: 0 error(s)`.
- `doctoc --dryrun --update-only` on the plan: "Everything is
  OK" (TOC current).

## Summary

Both round-1 MINOR findings (M1 course-of-values precedent, M2
payload-positivity) and the NIT (N1 P4 phrasing) are correctly
resolved, verified against source at the pinned lines. The fresh
sweep surfaces one MINOR leftover from the M1 fix: the
File-structure import list (line 260) still names the demoted
`LawvereTreeERArith` module and omits
`GebLean/Utilities/ERTreeArith.lean`, where the plan's stated
anchor `ERMor1.foldBTLOnCode` actually lives and which no other
listed import transitively provides. Because the import list is
advisory and Task 6.4.12's re-checkpoint points at the correct
file, impact is bounded, but the inconsistency should be swept
for full convergence. Verdict: ITERATE.
