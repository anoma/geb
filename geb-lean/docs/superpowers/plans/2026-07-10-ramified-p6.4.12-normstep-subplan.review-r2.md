# Adversarial review R2: Task 6.4.12 `normStep` mini-sub-plan

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [Verdict summary](#verdict-summary)
- [Resolution of round-1 findings](#resolution-of-round-1-findings)
- [NIT findings](#nit-findings)
  - [R2-N1 — the default-arm example overclaims for `shiftCode`](#r2-n1--the-default-arm-example-overclaims-for-shiftcode)
  - [R2-N2 — consumed interfaces omit `ERMor1.oneN` and `ordCode_codeRType`](#r2-n2--consumed-interfaces-omit-ermor1onen-and-ordcode_codertype)
- [Checks that passed](#checks-that-passed)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

Document under review:
`docs/superpowers/plans/2026-07-10-ramified-p6.4.12-normstep-subplan.md`
at commit `40571a97` on `feat/ramified-p6.4-normstep`. Round-2 scope:
resolution of every round-1 finding, absence of fix-induced defects,
and a fresh skeptical pass. Verified against the repository at `main`
pin `e76d3fc9a` (the two commits since are plan-document-only), the
round-1 review, the gate re-checkpoint
`.superpowers/sdd/task-6.4.12-report.md` (monorepo root), and the
mathlib pin at `.lake/packages/mathlib`.

## Verdict summary

| Severity | Count |
| --- | --- |
| BLOCKER | 0 |
| MAJOR | 0 |
| MINOR | 0 |
| NIT | 2 |

CONVERGED. All thirteen round-1 findings are resolved; no fix
introduced an inconsistency, stale cross-reference, or contradiction;
the fresh pass surfaced two NIT-severity inaccuracies and nothing of
MINOR severity or above. The plan is ready for user review.

## Resolution of round-1 findings

| Finding | Resolved | Evidence |
| --- | --- | --- |
| R1-M1 | yes | Task 6.4.12.1 Files field creates `GebLean/Utilities/ERCourseOfValues.lean` and `GebLeanTests/Utilities/ERCourseOfValues.lean` and modifies `GebLean/Utilities.lean` + `GebLeanTests.lean`; Step 1 wires both imports with a negative build check ("if it reports nothing, the index wiring is missing"); the File structure table carries both index rows. Wiring claims verified: `GebLean/Utilities.lean` is the per-directory index, `GebLean.lean:146` imports it, `GebLeanTests.lean:37` imports `GebLeanTests.Utilities.ERTreeArith` (the cited precedent), and the `GebLean` lib in `lakefile.toml` declares no `globs`. |
| R1-I1 | yes | M3's hypothesis set is `h_sane`, `hval`, `h_node`, `h_det`, `h_ext`, `h_fext`; `hval` is unconditional; `h_off` appears nowhere in the document (grep). Decision Q8's sentence now reads "makes the combinator's unconditional value-bound hypothesis (M3) trivial there", aligned with M3. Sufficiency and consumption re-verified below (Checks that passed). |
| R1-I2 | yes | Execution notes state 7 needs {1, 2}; 8 needs only 1; 9 needs {2, 7, 8}; 10 needs {2, 3, 5, 8, 9}; 11 needs {5, 6, 10}; 12 last. Each edge matches the task's consumed interfaces (Task 9: `shiftIterER`, `shiftCode_iterate_le_tower` from 7, reads from 2, `cvRecGated` from 8; Task 10: `betaRankER` from 5, `isLamER` from 2, `ordER` from 3, `subER` from 9, `cvRecGated` from 8; Task 11: `betaRankER`/`hasIotaER` from 5, `iotaStepER` from 6, `betaStepER` from 10). The parallel window {7, 8} beside the 3-4-5-6 chain is consistent. |
| R1-I3 | yes, with one residual inaccuracy | M5 carries the "Default-arm obligation" paragraph; the sanctioned style `rw [<def>]; split <;> simp_all [Nat.unpair_pair]` matches the landed node-equation proofs verbatim (e.g. `CodeTm.lean:301-316`, `CodeNormalizer.lean:185`); `ordCode_pair_zero` is pinned at `Nat.pair 0 0` exactly (`CodeTm.lean:301`). The example's universal claim about top tags `≥ 2` overclaims for `shiftCode` — see R2-N1. |
| R1-I4 | yes | Task 6.4.12.1 Step 4 and Task 6.4.12.8 Step 4 both home their smoke tests in `GebLeanTests/Utilities/ERCourseOfValues.lean`; the word "temporary" appears nowhere in the document (grep); no source-file `example` is sanctioned anywhere. |
| R1-N1 | yes | The Architecture paragraph reads "nine table folds realizing the `stepCode` ingredients (seven one-dimensional, two gated two-dimensional), each carrying its own universal majorant"; M5's table has exactly nine fold rows (seven 1-D, two gated 2-D) plus the parenthetical non-fold `shiftIterER`; "tower-2" is gone. |
| R1-N2 | yes | `normalCode` appears nowhere in the document (grep). |
| R1-N3 | yes | All eight renamed majorants (`ordCode_le_self`, `topBetaRankCode_le_succ`, `betaRankCode_le_succ`, `iotaStepCode_le_tower`, `shiftCode_le_tower`, `shiftCode_iterate_le_tower`, `subCode_shift_iterate_le_tower`, `betaStepCode_le_tower`) appear consistently across the M4 table, Decision Q5, the File structure table, the task interface blocks and prose, and the Execution-notes dependency bullet (34 occurrences). A regex sweep for the old truncated names finds zero residues. |
| R1-N4 | yes | "junk" appears nowhere; Decision Q8 reads "Indices outside the gate". |
| R1-N5 | yes | Task 6.4.12.8 Step 4 uses gate `i ≤ code / 2` with the run of checked positions including off-gate positions, exercising the `condN`-to-`1` path; extraction is at a gated position; the table is `0` off the gate per the Decision Q8 convention. |
| R1-N6 | yes | Task 6.4.12.8 Step 3 carries the `maxHeartbeats` expectation ("at least at the Task 6.4.12.1 level") with the cost rationale. |
| R1-N7 | yes | The Execution notes record the recipe deviation: the gate recipe's wave 11 routes `stepBoundER` through "`LawvereERTetration` precedents" (`task-6.4.12-report.md:206-207`, verified); the plan routes it through `ERMor1.towerER` + `interp_towerER`. |
| R1-N8 | yes | M3's closing paragraph cites `Nat.pair_lt_pair_left`/`Nat.pair_lt_pair_right` in `Mathlib/Data/Nat/Pairing.lean` for gated-index containment; both exist at the pin (`:112`, `:126`). |

## NIT findings

### R2-N1 — the default-arm example overclaims for `shiftCode`

- Location: M5, "Default-arm obligation" paragraph.
- Defect: the paragraph states "no landed equation covers the
  `| _, _ =>` arms of the term-code recursions at top tags `≥ 2`".
  This is true for `subCode`, `conHeadedCode`, `topBetaRankCode`,
  `iotaSpineCode`, `betaRankCode`, `hasIotaCode`, `betaStepCode`, and
  `iotaStepCode` (their landed families stop at the tag-`1` operation
  arms), but false for `shiftCode`: `shiftCode_pair_of_two_le`
  (`CodeNormalizer.lean:231`) and `shiftCode_of_two_le`
  (`CodeNormalizer.lean:237`) cover exactly the top-tag-`≥ 2` case.
  The round-1 finding carried the same overclaim and the fix
  transcribed it.
- Consequence: the obligation itself stands (eight of the nine
  recursive families need the sanctioned additions), but Task
  6.4.12.7's worker may author a duplicate of the landed `shiftCode`
  default-arm lemma, against the reuse rule.
- Fix: weaken the example to "most term-code recursions" or name
  `shiftCode_of_two_le` as the landed exception to reuse.

### R2-N2 — consumed interfaces omit `ERMor1.oneN` and `ordCode_codeRType`

- Location: Consumed interfaces section.
- Defect: Tasks 6.4.12.4 and 6.4.12.5 set
  `valBound := ERMor1.oneN 1`, and Task 6.4.12.3 Step 4's tests go
  "via `ordCode_codeRType`", but the Consumed interfaces section lists
  neither (`ERMor1.oneN` is at `GebLean/LawvereER.lean:264`;
  `ordCode_codeRType` at
  `GebLean/Ramified/Soundness/CodeTm.lean:323`). Both declarations
  exist at the pin, so nothing is unimplementable; the pin ledger is
  merely incomplete.
- Fix: add the two entries.

## Checks that passed

Fix-induced-defect sweep (no defects found):

- No double-wiring: Task 6.4.12.1 alone modifies `GebLeanTests.lean`;
  Task 6.4.12.2 alone modifies `GebLean/Ramified/Soundness.lean` and
  `GebLeanTests/Ramified/Soundness.lean`; the File structure table's
  task column matches both Files fields row for row. The Soundness
  test index exists and is reachable
  (`GebLeanTests.lean:32` → `GebLeanTests/Ramified.lean:13` →
  `GebLeanTests/Ramified/Soundness.lean`).
- Decision Q5's homing bullet, Task 6.4.12.1's Files field, and the
  File structure table agree on who creates and who modifies each of
  the ten files; Task 6.4.12.8 inherits both `Utilities` files as
  Modify only.
- No section still references the dropped `h_off` or the gated-`hval`
  packaging; Task 6.4.12.8's existence sketch ("the reference table
  (zero off the gate, `hval` on it) has a β-witness below the range")
  is consistent with M3's unconditional `hval` — witnessing the table
  bounded on all of `[0, N]` discharges existence with the ungated
  `bprod` positions vacuous.
- The revised plan passes `markdownlint-cli2` (0 errors); the new
  prose (Default-arm obligation, Execution-notes graph and deviation,
  Task 1/8 step text) is formal and colloquialism-free.
- New-text citations are accurate: `Nat.pair_lt_pair_left` :112 and
  `Nat.pair_lt_pair_right` :126 at the mathlib pin; the
  `GebLeanTests/Utilities/ERTreeArith.lean` wiring precedent at
  `GebLeanTests.lean:37`; the `lakefile.toml` no-`globs` claim for the
  `GebLean` lib; `towerER`/`interp_towerER` in
  `GebLean/LawvereERBoundComputable.lean` (:230/:240 region).

Fresh-pass checks (all sound):

- M3 hypothesis set: sufficient and consumed. `h_sane` is required —
  `ERMor1.condN` (`ERArith.lean:138`) is specified only on switch
  values `0`/`1` ("otherwise returns a well-defined but unspecified
  value"), so the gated predicate body needs `S i ≤ 1`. `hval` feeds
  `Nat.bounded_beta_exists` (`ERArith.lean:778`), which requires the
  whole table `[0, N]` bounded. `h_node` closes the gated `bprod`
  clauses for the witness; `h_det` is determinacy; `h_ext` keeps the
  extraction inside the table; `h_fext` discharges the `min`-clamp.
  The one redundancy (`h_fext` derivable from `hval` + `h_ext`) is
  already declared packaging latitude in M3 itself.
- Decision Q6 versus M2/M3: no contradiction. M2's `h_node` premise
  (`p < i`) matches the 1-D strict-descent argument; M3's premise
  (`p ≤ N`) matches the gated variant where determinacy is
  client-supplied; Q6 states exactly this split.
- The term-code `_var` equations are total over top-shape-`0` codes
  (Szudzik pairing is bijective, so every code with
  `(Nat.unpair c).1 = 0` is `Nat.pair 0 i`); `ordCode_pair_zero` is
  genuinely pinned at `Nat.pair 0 0` (shape-`0` codes with nonzero
  payload fall to the default arm).
- Task 6.4.12.8 smoke test: with gate `i ≤ code / 2` and node
  `proj 0`, the table `f i = if i ≤ code / 2 then i else 0` satisfies
  the Decision Q8 off-gate-zero convention; a gated extraction
  position satisfies `h_ext` for any `idxBound` at or above the fold
  slot; `h_det` at a gated `X` follows by instantiating the premise at
  `i = X`. The unspecified `idxBound`/`valBound` choices are
  implementer latitude consistent with M3.
- Gate preservation re-checked: application descent
  `Nat.pair d child` and abstraction descent `Nat.pair (d + 1) body`
  both stay on the gate (`child < m`; `body + 1 ≤ m` forces
  `(d + 1) + body ≤ d + m ≤ c`) and below
  `idxBound = Nat.pair c c` (pair monotonicity); the extraction
  position `Nat.pair 0 c` is gated (`0 + c ≤ c`).
- `stepBound n = tower 2 (6 * (2 * (n + 1) ^ 2 + n + 1))`
  (`CodeNormalizer.lean:2221`) matches Task 6.4.12.11's polynomial;
  `stepCode c = min (stepCodeAt 0 c) (stepBound c)`
  (`CodeNormalizer.lean:2229`) matches the M6 assembly; `stepCodeAt`'s
  guards (`0 < betaRankCode c`, then `hasIotaCode c`) match
  `stepCodeAtZeroER`'s `signN`/`condN` dispatch.
- `valBound` domination re-checked on the gates:
  `18m + 9e + 9d + 18 ≤ 27c + 9e + 18` and
  `27m + 9d + 18 ≤ 36c + 18` at `d, m ≤ c`; the 1-D `valBound`s
  (`c`, `1`, `c + 1`, `tower 2 (9c + 9)`) dominate their M4 majorants
  pointwise and are monotone in the fold slot (M2's `h_mono`).
- Slot orders are consistent between M5, the task interface blocks,
  and the consumers: `shiftER ![m, j]`, `shiftIterER ![d, j, e]`,
  `subER ![c, j, e]` with the contraction call at `(body, d, arg)`
  matching `subCode d arg body`, `betaStepER ![c, q]` with extraction
  at depth `0` matching the `stepCodeAt 0` consumer.
- Consumed-interface line citations re-verified at the pin (spot
  checks: `subCode` :312, `isLamCode` :743, `conHeadedCode` :801,
  `topBetaRankCode` :902, `iotaSpineCode` :1000, `resultShapeCode`
  :1163, `topIotaCode` :1201, `betaRankCode` :1288, `hasIotaCode`
  :1423, the node reads :1590-:1645, `betaStepCode` :1701 with
  equations :1741-:1775, `iotaStepCode` :1788, `stepCodeAt` :2211,
  `stepBound` :2221, `stepCode` :2229).
- All eighteen task commit messages remain imperative, lowercase,
  unterminated, `<type>(<scope>): <subject>`, and within 72
  characters.
